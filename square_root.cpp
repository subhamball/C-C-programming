#include<iostream>
#include<cmath>
using namespace std;

int main(){

    double number,sq_root;
    cout<<"enter a number: ";
    cin>>number;

    sq_root = sqrt(number);
    cout<<"square root is: "<<sq_root<<endl;

    return 0;

}